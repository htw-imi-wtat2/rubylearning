describe "Arrays" do

	let(:arrayX) {[0,1,2,3]}
	let(:arrayStr) {["Name","Nachname","Vorname"]}

	it "elements can be accessed" do
		expect(arrayX[0]).to eq(0)
	end

	it "adding elements"


	it "deleting element"

end
